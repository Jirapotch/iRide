# Architecture decision records

ADRs capture choices that later implementation steps may rely on. Records are immutable once accepted; superseding a choice requires a new ADR that links to the previous record.
