import { redirect } from "next/navigation";
import { Check, LockKeyhole, UserRoundX } from "lucide-react";
import { respondToFollowRequestAction, updatePrivacyAction } from "@/app/actions";
import { AppHeader } from "@/components/app-header";
import { LanguageSettings } from "@/components/settings-controls";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { getMyProfile, getPendingFollowRequests, getViewerContext } from "@/lib/data";
import { getLocale } from "@/lib/i18n-server";
import { initials } from "@/lib/profile-utils";

export default async function SettingsPage() {
  const [locale, viewer] = await Promise.all([getLocale(), getViewerContext()]);
  if (!viewer) redirect(`/auth?next=${encodeURIComponent("/settings")}`);
  if (!viewer.onboardingCompleted) redirect("/settings/profile");
  const [profile, requests] = await Promise.all([getMyProfile(), getPendingFollowRequests()]);
  if (!profile) redirect("/settings/profile");
  return <><AppHeader locale={locale} /><main className="mx-auto w-full max-w-3xl space-y-6 px-4 py-10 sm:px-6"><div><h1 className="text-3xl font-bold">{locale === "th" ? "ตั้งค่า" : "Settings"}</h1><p className="mt-1 text-muted-foreground">{locale === "th" ? "จัดการความเป็นส่วนตัวและภาษาของคุณ" : "Manage your privacy and language."}</p></div><Card className="surface-shadow border-white/70"><CardHeader><CardTitle className="flex items-center gap-2"><LockKeyhole className="size-5 text-primary" />{locale === "th" ? "ความเป็นส่วนตัว" : "Privacy"}</CardTitle><CardDescription>{locale === "th" ? "บัญชีส่วนตัวจะแสดง Vehicle และโพสต์ให้เฉพาะผู้ติดตามที่คุณอนุมัติ" : "A private account shows vehicles and posts only to followers you approve."}</CardDescription></CardHeader><CardContent><form action={updatePrivacyAction} className="flex items-center justify-between gap-4"><div><Label htmlFor="isPrivate" className="text-base">{locale === "th" ? "บัญชีส่วนตัว" : "Private account"}</Label><p className="mt-1 text-sm text-muted-foreground">{locale === "th" ? "ข้อมูลโปรไฟล์พื้นฐานยังคงมองเห็นได้" : "Basic profile information remains visible."}</p></div><div className="flex items-center gap-3"><Input id="isPrivate" name="isPrivate" type="checkbox" defaultChecked={profile.isPrivate} className="size-5" /><Button type="submit" variant="outline">{locale === "th" ? "บันทึก" : "Save"}</Button></div></form></CardContent></Card><Card className="surface-shadow border-white/70"><CardHeader><CardTitle>{locale === "th" ? "ภาษา" : "Language"}</CardTitle><CardDescription>{locale === "th" ? "การตั้งค่านี้จะใช้กับบัญชีของคุณ" : "This preference follows your account."}</CardDescription></CardHeader><CardContent><LanguageSettings locale={locale} /></CardContent></Card><Card className="surface-shadow border-white/70"><CardHeader><CardTitle>{locale === "th" ? "คำขอติดตาม" : "Follow requests"}</CardTitle><CardDescription>{locale === "th" ? `${requests.length} คำขอที่รอการตอบรับ` : `${requests.length} pending request${requests.length === 1 ? "" : "s"}`}</CardDescription></CardHeader><CardContent className="space-y-3">{requests.length ? requests.map((request) => <div key={request.followerId} className="flex items-center gap-3 rounded-xl border p-3"><Avatar><AvatarImage src={request.avatarUrl ?? undefined} alt={request.displayName} /><AvatarFallback>{initials(request.displayName)}</AvatarFallback></Avatar><div className="min-w-0 flex-1"><p className="truncate font-medium">{request.displayName}</p><p className="truncate text-sm text-muted-foreground">@{request.username}</p></div><form action={respondToFollowRequestAction.bind(null, request.followerId, "accept")}><Button size="icon" aria-label={locale === "th" ? "อนุมัติคำขอ" : "Accept request"}><Check className="size-4" /></Button></form><form action={respondToFollowRequestAction.bind(null, request.followerId, "reject")}><Button size="icon" variant="outline" aria-label={locale === "th" ? "ปฏิเสธคำขอ" : "Reject request"}><UserRoundX className="size-4" /></Button></form></div>) : <p className="py-6 text-center text-sm text-muted-foreground">{locale === "th" ? "ยังไม่มีคำขอติดตาม" : "No pending follow requests."}</p>}</CardContent></Card></main></>;
}
