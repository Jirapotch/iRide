import Link from "next/link";
import { CarFront, ImagePlus, MapPin, PenLine, Users } from "lucide-react";
import { AppHeader } from "@/components/app-header";
import { PostCard } from "@/components/post-card";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { demoProfile } from "@/lib/demo-data";
import { getFeed } from "@/lib/data";
import { assertLocale, getDictionary } from "@/lib/i18n";

export default async function FeedPage({ params }: { params: Promise<{ locale: string }> }) {
  const locale = assertLocale((await params).locale); const dict = getDictionary(locale); const posts = await getFeed();
  return <><AppHeader locale={locale} /><main className="mx-auto grid w-full max-w-6xl gap-6 px-4 py-6 sm:px-6 lg:grid-cols-[220px_minmax(0,620px)_240px]"><aside className="hidden lg:block"><Card className="sticky top-22 border-white/70 bg-card/90"><CardContent className="space-y-5 p-5"><Avatar className="size-14"><AvatarImage src={demoProfile.avatarUrl ?? undefined} /><AvatarFallback>NC</AvatarFallback></Avatar><div><p className="font-semibold">{demoProfile.displayName}</p><p className="text-xs text-muted-foreground">@{demoProfile.username}</p></div><div className="flex gap-5 text-xs"><div><strong className="block text-sm">{demoProfile.followersCount}</strong>Followers</div><div><strong className="block text-sm">2</strong>Cars</div></div><Button asChild variant="outline" className="w-full"><Link href={`/${locale}/profile/${demoProfile.username}`}>View profile</Link></Button></CardContent></Card></aside><section className="min-w-0 space-y-4"><div className="mb-2"><h1 className="text-2xl font-bold tracking-tight">{dict.feed.title}</h1><p className="text-sm text-muted-foreground">{dict.feed.subtitle}</p></div><Card className="surface-shadow border-white/70 bg-card/95"><CardContent className="flex items-center gap-3 p-4"><Avatar className="size-10"><AvatarImage src={demoProfile.avatarUrl ?? undefined} /><AvatarFallback>NC</AvatarFallback></Avatar><Button asChild variant="secondary" className="h-10 flex-1 justify-start rounded-full px-4 font-normal text-muted-foreground"><Link href={`/${locale}/post/new`}>{dict.feed.composer}</Link></Button><Button asChild size="icon" variant="ghost"><Link href={`/${locale}/post/new`} aria-label="Add photo"><ImagePlus className="size-5 text-primary" /></Link></Button></CardContent></Card>{posts.length ? posts.map((post) => <PostCard key={post.id} post={post} locale={locale} />) : <Card><CardContent className="grid place-items-center gap-3 p-12 text-center"><PenLine className="size-8 text-primary" /><h2 className="font-semibold">No stories yet</h2><p className="text-sm text-muted-foreground">Be the first to share a moment from the road.</p></CardContent></Card>}</section><aside className="hidden xl:block"><div className="sticky top-22 space-y-4"><Card className="border-white/70 bg-card/90"><CardHeader><CardTitle className="text-base">Around you</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-xl bg-primary/10 text-primary"><CarFront className="size-4" /></div><div><p className="font-medium">Sunday Cars & Coffee</p><p className="text-xs text-muted-foreground">Bangkok · 46 drivers</p></div></div><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-xl bg-primary/10 text-primary"><MapPin className="size-4" /></div><div><p className="font-medium">Mae Rim morning run</p><p className="text-xs text-muted-foreground">Chiang Mai · Saturday</p></div></div></CardContent></Card><Card className="border-dashed bg-transparent"><CardContent className="space-y-3 p-5 text-center"><Users className="mx-auto size-6 text-primary" /><p className="text-sm font-medium">The feed gets better with every driver.</p><Button asChild variant="outline" size="sm"><Link href={`/${locale}/auth`}>Invite a friend</Link></Button></CardContent></Card></div></aside></main></>;
}
